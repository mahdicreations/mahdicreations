# dependencies
/node_modules

# build output (must be built locally before deploy, or built on server)
# Note: For many static site hosts (Netlify, Vercel, Cloudflare), you commit the source,
# and they run `npm run build` which generates /dist. So /dist is usually ignored.
/dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.*.local